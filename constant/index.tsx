const prefix = '/frontend'

const API = {
  LOG_IN: `${prefix}/auth/login`,
  REGISTER: `${prefix}/auth/register`,
  ALL_CATEGORY: `${prefix}/category`,
  ALL_POST: `${prefix}/post/all`,
  POST: `${prefix}/post`,
  ME: `${prefix}/auth/me`,
  FILE: `/files`,
}

export { API }
