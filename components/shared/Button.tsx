import { Button as AntdButton } from 'antd'
import styled from 'styled-components'

const Button = styled(AntdButton)`
  background: #25a1b1 !important;
  height: unset;
  border-radius: 8px;
  padding: 7px 0;
`

export default Button
